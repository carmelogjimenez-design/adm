========================================================
  ADM - Proyecto Next.js completo (base + conexion Supabase)
========================================================

Este ZIP contiene el proyecto entero y buildeable. Sube TODO
su contenido a la raiz del repositorio "adm".

COMO SUBIRLO (igual que antes):
  1. Descomprime el ZIP.
  2. En GitHub:  Add file -> Upload files.
  3. Arrastra TODO lo de dentro: las carpetas "app" y "lib",
     y los archivos sueltos (package.json, tsconfig.json,
     next.config.mjs, postcss.config.mjs, tailwind.config.ts,
     middleware.ts, .gitignore).
     NO subas este LEEME.txt.
  4. Si GitHub avisa de que app/ o lib/ ya existen, perfecto:
     los reemplaza/fusiona sin borrar nada.
  5. Mensaje de commit (p.ej. "chore: base del proyecto Next.js")
     -> Commit changes.

DESPUES:
  - Vercel detectara Next.js y construira solo. Espera "Ready".
  - Comprueba en Vercel -> Settings -> Environment Variables:
      NEXT_PUBLIC_SUPABASE_URL
      NEXT_PUBLIC_SUPABASE_ANON_KEY
  - Asegura tu perfil admin en Supabase (SQL Editor), con tu UID
    de Authentication -> Users:

      insert into public.profiles (id, full_name, role)
      values ('TU_UID', 'Carmelo', 'admin')
      on conflict (id) do update set role = 'admin';

  - Entra en  https://TU-DOMINIO.vercel.app/login  e inicia sesion.
    Deberias ver /panel con tu rol admin y las 15 fases.

NOTA: el archivo LEEME.txt que ya subiste antes puedes borrarlo
desde GitHub (no molesta, pero no hace falta).
